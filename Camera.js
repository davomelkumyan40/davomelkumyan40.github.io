import Behaviour from "./Behaviour.js";

export default class Camera extends Behaviour {

    constructor(object) {
        super(object);
    }

    update() {
        super.update();
    }


    fixedUpdate() {
        super.update();
    }
}